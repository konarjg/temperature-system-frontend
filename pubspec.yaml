name: your_project_name
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

flutter: 
  uses-material-design: true

environment:
  sdk: ">=3.0.0 <4.0.0" # Kept wide to allow older build libraries

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  # API Client Dependencies
  dio: ^5.9.0
  # Downgraded to match the generator version below
  built_value: ^8.9.2
  built_collection: ^5.1.1
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  built_value_generator: 8.9.2 
  build_runner: 2.4.13 